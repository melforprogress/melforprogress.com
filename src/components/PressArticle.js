import React from 'react'

import styles from './PressArticle.module.css'

export default function({title, source, href, date, imageSrc, blurb}){
  return <div className={styles.article}>
      Content
  </div>
}
